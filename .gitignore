build/
*.bmp
*.png
!assets/**/*.png
!assets/**/*.bmp
!metadata/screenshots/**/*.bmp
!metadata/screenshots/**/*.png
!metadata/icon.png
!src/playdate/Source/thumbnails/**/*.png
*.o
*.obj
*.exe
.claude/worktrees/

# Playdate build outputs / Simulator runtime artifacts - regenerated by
# `cmake --build build` (packrom's own `pdc` post-build step) and by
# actually running PlaydateSimulator.exe against the built .pdx, not
# checked-in source.
*.pdx/
src/playdate/Source/pdex.dll
.sentry-native/
